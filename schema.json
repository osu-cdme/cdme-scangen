{
  "Input": [
    {
      "name": "Scan Strategy",
      "type": "string",
      "desc": "Which scan strategy you'd like to be used on the part.",
      "options": ["default", "island"],
      "default": "default"
    },
    {
      "name": "Part File Name",
      "type": "string",
      "desc": "Which file you'd like to print. If your file isn't here, import it from the 'Import/Export' menu!",
      "options": [],
      "default": "nut.stl"
    }
  ],
  "Hatch Default ID": "default", 
  "Contour Default ID": "default", 
  "Segment Styles": [
    {
      "id": "default", 
      "velocityProfileID": "default",
      "laserMode": "Independent", 
      "travelers": [
        {
          "id": 0, 
          "syncDelay": 0, 
          "power": 100, 
          "spotSize": 1, 
          "wobble": {
            "on": 1, 
            "freq": 100, 
            "shape": 0, 
            "transAmp": 1, 
            "longAmp": 1
          }
        }
      ]
    }
  ], 
  "Velocity Profiles": [
    {
      "id": "default",
      "velocity": 100, 
      "mode": "Auto", 
      "laserOnDelay": 20, 
      "laserOffDelay": 20, 
      "jumpDelay": 20, 
      "markDelay": 20, 
      "polygonDelay": 20
    }
  ],
  "Output": [],
  "Hatching": [
    {
      "name": "Layer Thickness",
      "type": "float",
      "desc": "How thick each layer should be.",
      "units": "mm",
      "default": "1"
    },
    {
      "name": "Hatch Angle",
      "type": "float",
      "desc": "The angle to rotate per layer.",
      "units": "deg",
      "default": "66.7"
    },
    {
      "name": "Volume of Offset Hatch",
      "type": "float",
      "desc": "Offset between internal and external boundary (Need to confirm what this actually does).",
      "units": "mm",
      "default": "0.08"
    },
    {
      "name": "Spot Compensation",
      "type": "float",
      "desc": "Additional, multiplicative offset to account for laser spot size. Set to 1 for no change.",
      "units": "unitless",
      "default": "1"
    },
    {
      "name": "# Inner Contours",
      "type": "int",
      "desc": "Number of inner contours. (Need to confirm difference between inner and outer contours)",
      "default": "2"
    },
    {
      "name": "# Outer Contours",
      "type": "int",
      "desc": "Number of outer contours. (Need to confirm difference between inner and outer contours)",
      "default": "2"
    },
    {
      "name": "Hatch Sorting Method",
      "type": "string",
      "options": ["None", "Alternate", "Linear", "Greedy"],
      "desc": "Which way you'd like to sort the hatches.",
      "default": "Alternate"
    }
  ],
  "Trajectory Ordering": [
    {
      "name": "Contour First",
      "type": "bool",
      "options": ["Yes", "No"],
      "desc": "Whether to print contours before hatches each layer.",
      "default": "Yes"
    }
  ]
}
